name: dante-test
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - setuptools
  - wheel

  - pip:
      # ---------- Core ML ----------
      - transformers==4.47.1
      - tokenizers==0.21.1
      - sentencepiece==0.2.0
      - safetensors==0.5.3
      - timm==0.6.13
      - einops==0.8.1

      # ---------- Video / Vision ----------
      - decord==0.6.0
      - opencv-python==4.12.0.88
      - pillow==11.0.0

      # ---------- Data / Utils ----------
      - numpy==2.1.2
      - pandas==2.2.3
      - tqdm==4.67.1
      - pyyaml==6.0.2
      - omegaconf==2.3.0
      - rich==14.0.0
      - wandb==0.21.3
      - matplotlib==3.10.3
      - seaborn==0.13.2
      - iopath==0.1.10

      # ---------- Infra ----------
      - requests==2.32.3
      - packaging==25.0
      - typing-extensions==4.12.2
